# Replace the value with the output of `workshop-analytics key generate`
# (or `just key-generate`) before applying. This placeholder does not
# decode, so the service refuses to start with it, plainly.
TOKEN_SIGNING_KEY=replace-me-with-key-generate
